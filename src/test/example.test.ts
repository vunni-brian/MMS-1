/**
 * Placeholder test to verify the test runner is configured correctly.
 * Can be removed once real tests are in place.
 */
import { describe, it, expect } from "vitest";

describe("example", () => {
 it("should pass", () => {
 expect(true).toBe(true);
 });
});
